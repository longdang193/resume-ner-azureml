# Mock configurations for test fixtures
# Defines default settings for mocked components

tokenizer:
  fast:
    default_max_length: 512
    supports_offset_mapping: true
    cls_token_id: 101
    sep_token_id: 102
  slow:
    default_max_length: 512
    supports_offset_mapping: false
    cls_token_id: 101
    sep_token_id: 102

model:
  default_backbone: "distilbert-base-uncased"
  default_dropout: 0.1

platform_adapters:
  azure_ml:
    default_workspace: "test-workspace"
    default_experiment: "test-experiment"
  local:
    default_output_dir: "./outputs"

# Mock configurations for orchestration components
orchestration:
  ml_client:
    default_workspace_name: "test-workspace"
    default_datastore: "workspaceblobstore"
  jobs:
    default_compute_cluster: "cpu-cluster"
    default_experiment_name: "test-experiment"
  data_assets:
    default_name: "test-asset"
    default_version: "1"
    default_path: "/test/path"

# Mock configurations for subprocess execution
subprocess:
  default_returncode: 0
  default_timeout: 60
  capture_output: true

